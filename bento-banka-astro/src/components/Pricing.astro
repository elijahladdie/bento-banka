---
import PricingIsland from "./pricing/PricingIsland.astro";

const { t, locale = "en" } = Astro.props;
---

<section id="pricing" class="relative overflow-hidden px-4 py-20 sm:py-24">
	<div class="container mx-auto relative z-10">
		<div class="mx-auto mb-10 max-w-3xl text-center sm:mb-14">
			<p class="text-xs font-semibold uppercase tracking-[0.28em] text-[var(--text-gold)]">
				{t("landing.pricing.kicker", "Pricing")}
			</p>
			<h2 class="mt-3 text-3xl font-semibold text-[var(--text-primary)] sm:text-4xl lg:text-5xl">
			{t("landing.pricing.title")}
			</h2>
			<p class="mt-4 text-sm leading-7 text-[var(--text-secondary)] sm:text-base">
				{t("landing.pricing.subtitle")}
			</p>
		</div>

		<PricingIsland

			locale={locale}
			monthlyLabel={t("landing.pricing.monthly")}
			yearlyLabel={t("landing.pricing.yearly")}
			toggleLabel={t("landing.pricing.toggleLabel")}
			popularLabel={t("landing.pricing.popular")}
			trialLabel={t("landing.pricing.trial")}
			ctaLabel={t("landing.pricing.cta")}
			loadingTitle={t("landing.pricing.loadingTitle")}
			loadingBody={t("landing.pricing.loadingBody")}
			errorTitle={t("landing.pricing.errorTitle")}
			errorBody={t("landing.pricing.errorBody")}
			retryLabel={t("landing.pricing.retry")}
			freeLabel={t("landing.pricing.free")}
			billedLabel={t("landing.pricing.billed")}
			billingMonthLabel={t("landing.pricing.billingMonth")}
			billingYearLabel={t("landing.pricing.billingYear")}
		/>
	</div>
</section>